---
name: interview
argument-hint: [--light | --full] [作りたい機能や要件の概要]
description: 【通常は /interview-light か /interview-full を使う】要件定義インタビュー本体スキル。引数なしで状況自動判定（spec-lightがあれば--full補完、なければ--light/--fullを確認）。「インタビューして」「要件を聞いて」「仕様を詰めて」「ヒアリングして」等の自然言語呼び出しでも発動。上級者向け。
allowed-tools: AskUserQuestion, Write, Read, Glob, Grep, Bash
---

# Interview Skill

共通理解に達するまでインタビューし、仕様書を生成するスキル。
出力は `/feature-dev` や `/deep-plan` にそのまま渡せる形式。

## モード選択

引数でモードを切り替える:

- `--light`: **モックアップ・見積もり・商談用** の軽量モード（5問で完結）
- `--full`（デフォルト）: **本実装用** の詳細モード（既存5観点 + コスト・法令・LLMリスク・性能）

### 引数なしで呼ばれた場合の判断
- `docs/spec-light-*.md` が既にある → **--full モード**で軽量仕様を補完して詳細化
- 新規プロジェクトかつモックアップ前 → どちらを使うかユーザーに確認
- それ以外 → **--full モード**で実行

## フェーズ1: コードベース探索（既存プロジェクトの場合・両モード共通）

インタビューを始める**前に**、まずコードベースを探索する。

- プロジェクト構造、既存パターン、技術スタック、DB構成を把握
- CLAUDE.md、000_PROJECT_STATUS.md を確認
- **コードを読めば答えが出る質問は、ユーザーに聞かずに自分で調べる**

探索で得た情報は、インタビューの質問に反映する。
「このプロジェクトではSupabaseを使っていますが、この機能もSupabaseで実装しますか？」のように具体的に聞く。

---

## ▼ --light モード（モックアップ・商談用）

### 目的
モックアップ作成・見積もり作成・商談に必要な**最低限の情報**を集める。
DB詳細設計・セキュリティ実装詳細・パフォーマンス最適化は聞かない（受注後に `--full` で詰める）。

### 質問構成（5つの AskUserQuestion で完結）

#### Q1: 用途とクライアント文脈
- **これは「個人開発・趣味・プロトタイプ」か「他者が使うproduction運用」か?**（Hobby or work? = 最重要前提）
- クライアント業種・サービス内容
- クライアント規模（個人事業主 / 数店舗 / 中小企業 / 大手）
- 現状の運用方法（電話、紙、Excel、既存ツール）
- 月間想定件数・利用規模の概算

#### Q2: エンドユーザー
- 利用者の年齢層・技術リテラシー
- 利用デバイス（スマホ中心 / PC中心 / 混在）
- 利用シーン（事前予約 / 当日利用 / リマインド前提など）

#### Q3: 機能スコープ（MVP）
- 顧客側で必須機能（リスト化）
- 管理画面の機能（リスト化）
- **今回やらないこと**（スコープを明確に切る。例: 決済、会員機能、レビュー）

#### Q4: モックアップ範囲
- 何画面見せたい? （3〜5画面が現実的）
- 主要画面リスト（例: TOP / 予約フォーム / マイページ / 管理ダッシュボード）
- デザインの方向性・参考サイト

#### Q5: 制約・前提
- おおまかな予算感
- おおまかな納期感
- 技術的な希望・制約（既存システム連携、特定サービス指定など）

### 出力: `docs/spec-light-[機能名].md`

```markdown
# [機能名] 軽量仕様書（モックアップ・商談用）

## 用途
- [ ] 個人開発・趣味・プロトタイプ
- [x] production運用（他者が使う）

## クライアント情報
- 業種・サービス: 
- 規模: 
- 現状の運用: 
- 想定規模（月間件数等）: 

## 想定エンドユーザー
- 年齢層: 
- 技術リテラシー: 
- 利用デバイス: 
- 利用シーン: 

## MVP機能リスト
### 顧客側
- 

### 管理画面
- 

### 今回やらないこと（スコープ外・明確に切る）
- 

## モックアップ範囲
- 必要画面数: 
- 主要画面リスト: 
- デザイン方向性: 
- 参考サイト: 

## 制約・前提
- 予算感: 
- 納期感: 
- 技術的希望・制約: 

## 次のアクション
- HTMLでモックアップ作成（mockups/pattern-a/ 配下）
- 見積もりドラフト作成（docs/006_estimate.md）
- 受注後: `/interview --full` でこの仕様書を補完して詳細化
```

---

## ▼ --full モード（本実装用）

### 目的
本実装前に、要件・データ・セキュリティ・コスト・法令を**全て固める**。

### 既に `docs/spec-light-*.md` がある場合
そのファイルを読み込み、**共通項目はスキップして不足分のみヒアリング**する。
「Q1の用途・クライアント情報は spec-light で確認済みです。次に詳細を聞きます」のように案内する。

### フェーズ2: 徹底インタビュー

AskUserQuestionツールを使い、設計ツリーの各分岐を1つずつ歩く。

#### 質問の進め方
1. **まず全体像を確認する** — 何を作りたいか、なぜ作るか、誰が使うか（spec-lightがあればスキップ）
2. **設計ツリーの分岐を歩く** — 各判断ポイントで選択肢を提示し、依存関係を1つずつ解決する
3. **深掘りする** — 各分岐について「もしXだったら？」「Yの場合は？」とエッジケースを確認
4. **共通理解に達するまで繰り返す** — 実装に必要な判断がすべて済むまで止めない

#### 質問の観点（9項目: 既存5 + 新規4）

##### 既存の5観点
- ユーザー体験（誰が、いつ、どう使うか）
- データ設計（何を保存するか、関連は、制約は）
- 技術的判断（認証、API設計、外部サービス連携）
- エッジケース（エラー時、データ不整合、権限）
- 優先順位（MVP vs フル機能、段階的リリース）

##### 新規追加の4観点（general-prerequisitesより）
- **コスト**: 外部API課金（OpenAI、Stripe、Supabase等）、polling loop、想定スケール時の月額試算、ハード上限の有無、kill switchの設計
- **法令・コンプライアンス**: 個人情報の取り扱い、GDPR/APPI、対象業種の規制（医療・金融・教育）、依存ライブラリのライセンス
- **LLMリスク（該当時のみ）**: データ経路（どのプロバイダ・どのリージョン）、Training opt-out状況、Prompt injection対策、LLMに与えるツール権限の範囲
- **性能の現実**: 1年後・3年後のデータ量見積もり、N+1パターンの予防、index設計、想定QPS

#### ルール
- 「当たり前すぎる質問」はしない（コードを見ればわかることは聞かない）
- 1回のAskUserQuestionで関連する質問を2〜4個まとめて聞く（1問ずつ聞かない）
- ユーザーが「完了」「もう大丈夫」と言うか、設計判断がすべて解決するまで続ける
- 判断に迷う箇所は選択肢を提示して選んでもらう
- LLMリスクは**LLMを使う案件のみ**問う（一般的なCRUDアプリでは不要）

### フェーズ3: 仕様書出力

インタビュー完了後、以下の形式で仕様書を `docs/spec-[機能名].md` に出力する。

```markdown
# [機能名] 仕様書

## 概要
（1〜2文で何を作るか）

## 実装前提
- 用途: 個人実験 / production運用
- コスト上限: $XXX/月（ハードキャップ あり / なし）
- 個人情報: 取り扱う / しない（GDPR/APPI対象 / 対象外）
- LLM利用: あり / なし
  - あれば: data path（プロバイダ・リージョン）, opt-out状況, Prompt injection対策

## 背景・目的
- なぜ作るか
- 誰が使うか
- 成功の定義

## 設計判断
| 判断ポイント | 決定 | 理由 |
|-------------|------|------|
| （例: 認証方式） | （例: Supabase Auth） | （例: 既存と統一） |

## 機能要件
### 必須（MVP）
- [ ] 要件1
- [ ] 要件2

### 将来対応
- [ ] 要件3

## データ設計
（テーブル、カラム、リレーション）
**性能見積もり**: 1年後 約X件、3年後 約Y件、想定QPS Z

## エッジケース
- ケース1: 対応方針
- ケース2: 対応方針

## 実装ステップ（推奨順序）
1. ステップ1（理由: 依存関係がないため先に着手可能）
2. ステップ2（理由: ステップ1に依存）

## 次のアクション
- `/deep-plan docs/spec-[機能名].md に基づいて計画を作成して`
- `/feature-dev docs/spec-[機能名].md の仕様に基づいて実装して`
```

---

## 使い方

```
# 軽量モード（モックアップ・商談用）
/interview --light 予約管理アプリを作りたい
/interview --light ECサイトのカート機能のモックアップ

# 詳細モード（本実装用、デフォルト）
/interview ログイン機能を作りたい
/interview --full 予約システムのキャンセル機能
/interview ECサイトのカート機能（spec-light読み込み→補完）
```

## 後続のフロー

```
[商談前]
/interview --light → docs/spec-light-*.md 生成
  ↓
HTMLモックアップ作成（mockups/pattern-a/）
  ↓
見積もりドラフト（docs/006_estimate.md）
  ↓
商談・受注

[受注後]
/interview --full → docs/spec-*.md 生成（spec-lightを読み込んで補完）
  ↓
DB設計（docs/004_database_schema.md）
  ↓
プロジェクトステータス + タスクバックログ
  ↓
実装開始
```

---

<instructions>$ARGUMENTS</instructions>

上記の指示に基づき、まずコードベースを探索し（既存プロジェクトの場合）、引数からモードを判定（--light / --full / 引数なしの判断ロジック）、その後AskUserQuestionツールを使って徹底的にインタビューしてください。

設計ツリーの各分岐を1つずつ歩き、依存関係を解決しながら共通理解に達するまで質問を続けてください。コードベースを探索すれば答えが出る質問は、ユーザーに聞かずに自分で調べてください。

インタビュー完了後、モードに応じたテンプレートに従って仕様書を出力してください:
- --light → `docs/spec-light-[機能名].md`
- --full → `docs/spec-[機能名].md`
